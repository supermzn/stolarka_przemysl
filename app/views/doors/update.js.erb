$('.submit').click(function() {
   $('#form-edit').hide('slow');
	 $('#edit_door').show('slow');
	 $('#cancel_edit_door').hide();
   $('#door_description').show('slow');//replaceWith("<%= j render 'show' %>");
});
